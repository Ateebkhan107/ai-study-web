import fs from"node:fs/promises";import path from"node:path";import process from"node:process";import Papa from"papaparse";import{createClient}from"@supabase/supabase-js";
process.loadEnvFile(".env.local");const APPLY=process.argv.includes("--apply"),ROOT=process.cwd(),DIR=path.join(ROOT,"tmp/neet-2017/structured"),DATASET=path.join(DIR,"neet-2017-structured-draft.json"),CSV=path.join(DIR,"neet-2017-structured.csv"),REPORT=path.join(DIR,"publish-report.json"),fields=["question_image","option_a_image","option_b_image","option_c_image","option_d_image"];
for(const k of["NEXT_PUBLIC_SUPABASE_URL","SUPABASE_SERVICE_ROLE_KEY"])if(!process.env[k])throw new Error(`${k} required`);const db=createClient(process.env.NEXT_PUBLIC_SUPABASE_URL,process.env.SUPABASE_SERVICE_ROLE_KEY,{auth:{persistSession:false,autoRefreshToken:false}});
function review(q){const solution=q.explanation?`\n\n**Solution**\n\n${q.explanation}`:"",a=q.accepted_options.map(x=>x.toUpperCase());if(a.length>1)return`**Official accepted options: ${a.join(" and ")}**\n\n${a.map(x=>`${x}. ${q[`option_${x.toLowerCase()}`]}`).join("\n\n")}${solution}`;const x=q.correct_option,l=x.toUpperCase();return`**Correct option: ${l}**\n\n${q[`option_${x}`]}${q[`option_${x}_image`]?`\n\nThe corresponding diagram is shown with option ${l}.`:""}${solution}`}
async function upload(file,n,f){const object=`neet-ug-2017/code-a/structured/q${String(n).padStart(3,"0")}-${f.replaceAll("_","-")}.png`,body=await fs.readFile(file),{error}=await db.storage.from("pyq-images").upload(object,body,{contentType:"image/png",upsert:true});if(error)throw new Error(`Q${n} ${f}: ${error.message}`);return db.storage.from("pyq-images").getPublicUrl(object).data.publicUrl}
async function main(){const data=JSON.parse(await fs.readFile(DATASET,"utf8"));if(data.length!==180||data.some((q,i)=>q.number!==i+1))throw new Error("Expected 1..180");const bad=[];for(const q of data){for(const f of["question","option_a","option_b","option_c","option_d"])if(!String(q[f]||"").trim())bad.push(`Q${q.number} empty ${f}`);const s=[q.question,q.option_a,q.option_b,q.option_c,q.option_d].join(" ");if(/[�□]|[\uE000-\uF8FF]|[]/u.test(s))bad.push(`Q${q.number} glyph`);for(const f of fields)if(q[f])await fs.access(q[f])}if(bad.length)throw new Error(bad.join("\n"));const{data:old,error:oe}=await db.from("pyq_questions").select("*").eq("exam","NEET").eq("year",2017).eq("paper_code","NEET 2017 Code A").order("question_number");if(oe)throw oe;if(old.length&&!(old.length===180&&old.every((q,i)=>q.question_number===i+1)))throw new Error(`Existing Code A incomplete (${old.length})`);if(old.length)await fs.writeFile(path.join(DIR,"live-backup-before-repair.json"),JSON.stringify(old,null,2));const{data:exam,error:ee}=await db.from("pyq_questions").select("exam_id").eq("exam","NEET").not("exam_id","is",null).limit(1).single();if(ee)throw ee;const rows=[];for(const q of data){const r={exam:"NEET",exam_type:"NEET UG",year:2017,attempt:"NEET UG 2017",shift:"Single Shift",paper_code:"NEET 2017 Code A",exam_id:exam.exam_id,subject:q.subject,chapter:q.chapter,question_type:"MCQ",question:q.question,option_a:q.option_a,option_b:q.option_b,option_c:q.option_c,option_d:q.option_d,correct_option:q.correct_option.toUpperCase(),explanation:review(q),explanation_image:null,marks_positive:4,marks_negative:-1,question_number:q.number,display_order:q.number,status:"PUBLISHED"};for(const f of fields)r[f]=q[f]?(APPLY?await upload(q[f],q.number,f):q[f]):null;rows.push(r)}await fs.writeFile(CSV,Papa.unparse(rows,{newline:"\n"}));const report={mode:APPLY?"apply":"dry-run",operation:old.length?"update":"insert",total:180,subjects:Object.fromEntries(["Physics","Chemistry","Biology"].map(s=>[s,rows.filter(q=>q.subject===s).length])),tables:rows.filter(q=>q.question.includes("\n|")).length,questionImages:rows.filter(q=>q.question_image).length,optionImages:rows.reduce((n,q)=>n+fields.slice(1).filter(f=>q[f]).length,0),multipleAnswerQuestions:data.filter(q=>q.accepted_options.length>1).map(q=>q.number)};if(APPLY){const payload=old.length?rows.map((r,i)=>({...r,id:old[i].id})):rows;for(let i=0;i<180;i+=30){const query=old.length?db.from("pyq_questions").upsert(payload.slice(i,i+30),{onConflict:"id"}):db.from("pyq_questions").insert(payload.slice(i,i+30));const{error}=await query;if(error)throw error}const{data:v,error}=await db.from("pyq_questions").select("question_number,question,option_a,option_b,option_c,option_d,explanation,explanation_image,question_image,option_a_image,option_b_image,option_c_image,option_d_image").eq("exam","NEET").eq("year",2017).eq("paper_code","NEET 2017 Code A").order("question_number");if(error)throw error;report.verified={total:v.length,sequence:v.every((q,i)=>q.question_number===i+1),empty:v.filter(q=>[q.question,q.option_a,q.option_b,q.option_c,q.option_d].some(x=>!String(x||"").trim())).length,badReview:v.filter(q=>!/^\*\*(Correct option|Official accepted options):/.test(String(q.explanation||""))).length,explanationImages:v.filter(q=>q.explanation_image).length,questionImages:v.filter(q=>q.question_image).length,optionImages:v.reduce((n,q)=>n+fields.slice(1).filter(f=>q[f]).length,0)};if(report.verified.total!==180||!report.verified.sequence||report.verified.empty||report.verified.badReview||report.verified.explanationImages||report.verified.questionImages!==7||report.verified.optionImages!==20)throw new Error(`Verification failed ${JSON.stringify(report.verified)}`)}await fs.writeFile(REPORT,JSON.stringify(report,null,2));console.log(JSON.stringify(report,null,2))}
main().catch(e=>{console.error(e);process.exit(1)});
