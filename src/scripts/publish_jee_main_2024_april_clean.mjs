import fs from 'node:fs/promises'; import {createClient} from '@supabase/supabase-js'; process.loadEnvFile('.env.local');
const s=createClient(process.env.NEXT_PUBLIC_SUPABASE_URL,process.env.SUPABASE_SERVICE_ROLE_KEY,{auth:{persistSession:false,autoRefreshToken:false}}); const code=process.argv[2];
const m=code.match(/^JEE-MAIN-24-(\d\d)APR-S([12])$/); if(!m)throw new Error('paper code required'); const day=m[1],shift=m[2], data=JSON.parse(await fs.readFile(`tmp/jee-main-2024-april-clean/${code}/manifest.json`,'utf8')); if(data.length!==90)throw new Error('incomplete');
const exam={exam:'JEE',exam_type:'JEE Main',year:2024,attempt:`${Number(day)} Apr`,shift:`Shift ${shift}`,paper_code:code,exam_date:`2024-04-${day}`,duration_minutes:180,total_marks:300,status:'PUBLISHED',is_published:true}; let {data:e}=await s.from('pyq_exams').select('id').eq('paper_code',code).maybeSingle(); if(!e){const r=await s.from('pyq_exams').insert(exam).select('id').single();if(r.error)throw r.error;e=r.data}else await s.from('pyq_exams').update(exam).eq('id',e.id);
for(const q of data){const p=`jee-main-2024-april-clean/${code}/q${String(q.number).padStart(2,'0')}.png`;const u=await s.storage.from('pyq-images').upload(p,await fs.readFile(q.image_path),{contentType:'image/png',upsert:true});if(u.error)throw u.error;const img=s.storage.from('pyq-images').getPublicUrl(p).data.publicUrl;const num=q.question_type==='NUMERICAL';const ans=String(q.answer);const row={exam_id:e.id,exam:'JEE',exam_type:'JEE Main',year:2024,attempt:exam.attempt,shift:exam.shift,paper_code:code,question_number:q.number,display_order:q.number,subject:q.subject,chapter:'Unmapped',question_type:q.question_type,question:`Question ${q.number}: Refer to the source image.`,option_a:'Option 1',option_b:'Option 2',option_c:'Option 3',option_d:'Option 4',correct_option:num?'a':'abcd'[Number(ans)-1]||'a',numerical_answer:num&&ans!=='DROP'?Number(ans):null,explanation:`Official NTA answer key: ${ans}.`,question_image:img,status:'PUBLISHED',marks_positive:4,marks_negative:num?0:1};const old=await s.from('pyq_questions').select('id').eq('paper_code',code).eq('question_number',q.number).maybeSingle();const r=old.data?await s.from('pyq_questions').update(row).eq('id',old.data.id):await s.from('pyq_questions').insert(row);if(r.error)throw r.error} console.log(code);
